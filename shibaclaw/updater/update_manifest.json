{
  "version": "0.0.31",
  "release_notes": "- **Hotfix** — Fixed missing `_BoundedBuffer` class that broke all `exec` tool calls. Raised Docker memory limits from 512 MB to 1 GB to prevent OOM kills during heavy installs (e.g. ffmpeg).",
  "upgrade_notes": "Restart the WebUI after upgrading.",
  "changes": [
    {
      "path": "shibaclaw/agent/tools/shell.py",
      "overwrite": true,
      "note": "Added _BoundedBuffer class that was missing from v0.0.30."
    },
    {
      "path": "CHANGELOG.md",
      "overwrite": true,
      "note": "Release notes for v0.0.31."
    }
  ]
}