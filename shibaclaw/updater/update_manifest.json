{
  "version": "0.0.23",
  "release_notes": "- **Bug fix**: WebUI agent message/attachment delivery now works correctly — messages sent via the `message()` tool are properly routed to the active session room instead of being silently dropped.",
  "upgrade_notes": "Restart the WebUI or Docker stack after upgrading.",
  "changes": [
    {
      "path": "shibaclaw/webui/agent_manager.py",
      "overwrite": true,
      "note": "Fix _consume_outbound: session lookup by session_key, emit to correct room, correct persist logic"
    }
  ]
}